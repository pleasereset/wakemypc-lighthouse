To install the service manually using the .net tools :
- Open a "Developer Command Prompt" or locate InstallUtil.exe with Administrator privilèges
- Run : InstallUtil /LogToConsole=true WakeMyPC.WindowsService.exe